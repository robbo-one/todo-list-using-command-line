#!/usr/bin/env node

const commands = require('./commands')

const userInputs = process.argv
const cmd = userInputs[2]
let id 
// console.log(userInputs)

switch (cmd) {
  case 'list':
    commands.list()
    break
  case 'done':
    id = userInputs[3] // This triggers if 'done' is input into cmd line.
    commands.done(id)  // runs done function in command.js file
    break
  case 'update':
    id = userInputs[3]
    message = userInputs[4] //takes message
    commands.update(id, message)//initiates update function in command.js
    break
  case 'search':
    let searchItem = userInputs[3] 
      commands.search(searchItem)
      break
    
  default:
    console.log(`I don't understand that command: ${cmd}`)
}
